{
  "name": "tabnews",
  "version": "0.0.1",
  "description": "Implementation for curso.dev",
  "main": "index.js",
  "scripts": {
    "dev": "npm run compose:up && npm run db:migration:up && next dev",
    "test": "npm run compose:up && concurrently -n next,jest --hide next -k -s command-jest \"next dev\" \"jest --runInBand\"",
    "test:watch": "npm run db:migration:up && concurrently -n next,jest --hide next -k -s command-jest \"next dev\" \"jest --watchAll --runInBand\"",
    "lint:prettier:check": "prettier --check .",
    "lint:prettier:fix": "prettier --write .",
    "lint:eslint:check": "next lint --dir .",
    "compose:up": "docker compose -f infra/compose.yaml up -d",
    "compose:stop": "docker compose -f infra/compose.yaml stop",
    "compose:down": "docker compose -f infra/compose.yaml down",
    "db:migration:create": "node-pg-migrate -m infra/migrations create",
    "db:migration:up": "npm run db:wait-postgres:up && node-pg-migrate -m infra/migrations --envPath .env.development up",
    "db:migration:down": "node-pg-migrate -m infra/migrations --envPath .env.development down",
    "db:wait-postngres:up": "node infra/scripts/wait-for-db.js",
    "prepare": "husky",
    "commit": "cz"
  },
  "author": "coderCadu",
  "license": "MIT",
  "dependencies": {
    "async-retry": "1.3.3",
    "dotenv": "16.4.5",
    "dotenv-expand": "11.0.7",
    "next": "15.0.3",
    "node-pg-migrate": "6.2.2",
    "pg": "8.13.1",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@commitlint/cli": "19.5.0",
    "@commitlint/config-conventional": "19.5.0",
    "commitizen": "4.3.1",
    "concurrently": "9.1.0",
    "cz-conventional-changelog": "3.3.0",
    "eslint": "8.57.0",
    "eslint-config-next": "15.0.3",
    "eslint-config-prettier": "9.1.0",
    "eslint-plugin-jest": "28.9.0",
    "husky": "9.1.6",
    "jest": "29.7.0",
    "prettier": "3.3.3"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
