{
  "name": "tabnews",
  "version": "0.0.1",
  "description": "Implementation for curso.dev",
  "main": "index.js",
  "scripts": {
    "dev": "docker compose -f infra/compose.yaml up -d && next dev",
    "up": "docker compose -f infra/compose.yaml up -d",
    "down": "docker compose -f infra/compose.yaml down",
    "lint:check": "prettier --check .",
    "lint:fix": "prettier --write .",
    "test": "jest",
    "test:watch": "jest --watch",
    "prepare": "husky install"
  },
  "author": "coderCadu",
  "license": "MIT",
  "husky": {
    "hooks": {
      "pre-commit": "npm run down && npm run lint:check && npm run test"
    }
  },
  "dependencies": {
    "next": "^13.1.6",
    "pg": "^8.11.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "husky": "^9.1.4",
    "jest": "^29.6.2",
    "prettier": "^3.3.3"
  }
}
